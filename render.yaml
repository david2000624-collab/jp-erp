services:
  - type: web
    name: jp-proxy-erp
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_DIR
        value: /var/data
    disk:
      name: erp-data
      mountPath: /var/data
      sizeGB: 1
